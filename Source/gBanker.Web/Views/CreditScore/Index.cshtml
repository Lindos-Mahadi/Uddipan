
@{
    ViewBag.Title = "Credit Score";
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}


<script type="text/javascript">
    $(document).ready(function () {

        $("#txtDateFrom").datepicker(
            {
                dateFormat: "dd-M-yy",
                showAnim: "scale"
            });
        $("#txtDateTo").datepicker(
          {
              dateFormat: "dd-M-yy",
              showAnim: "scale"
          });
        $('#btnExport').click(function () {
                var Datefrom  = $("#txtDateFrom").val();
                var DateTo = $("#txtDateTo").val();
                window.location = '/CreditScore/ExportData?Datefrom=' + Datefrom + '&DateTo=' + DateTo;

        });
        //$("#btnView").click(function (e) {
        //    e.preventDefault();

        //    var Date = $("#txtToDt").val();
        //    var Qtype = $("#ddlQtype").val();

        //    //var url = '/CreditScore/ExportData?Date=' + Date + '&Qtype=' + Qtype;

        //    var url = '/CreditScore/ExportData?Date=' + Date ;

        //    window.open(url, 'mywindow', 'fullscreen=yes, scrollbars=auto');
        //});
    });
</script>
<h2 class="page-title">Credit Score</h2>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="">
  
      
        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    @Html.Label("DateFrom", new { @class = "control-label" })
                    @Html.TextBox(" ", ViewData["Trxdate"].ToString(), new { @ID = "txtDateFrom", @class = "form-control" })
                    @*@Html.TextBox(" ", "", new { @ID = "txtToDt", @class = "form-control" })*@
                </div>
            </div>
            <div class="col-md-8">&nbsp;</div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    @Html.Label("DateTo", new { @class = "control-label" })
                    @Html.TextBox(" ", ViewData["TrxdateTo"].ToString(), new { @ID = "txtDateTo", @class = "form-control" })
                    @*@Html.TextBox(" ", "", new { @ID = "txtToDt", @class = "form-control" })*@
                </div>
            </div>
            <div class="col-md-8">&nbsp;</div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div class="form-group from-action">
                    <input type="button" id="btnExport" value="Export" class="btn btn-primary" />
                </div>
            </div>
        </div>

    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryui")
}

